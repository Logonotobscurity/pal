---
applyTo: '**/web/**'
---

# Frontend instructions

> Adjust the `applyTo` glob to match the actual frontend path.

- Treat `docs/PAL_DOMAIN_MODEL.md` as the source of truth for API shapes and states; never assume fields.
- No new UI dependencies without justification.
- All user-visible strings through the i18n layer (once established).
